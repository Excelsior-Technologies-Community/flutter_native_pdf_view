import 'package:flutter/material.dart';

class PdfLoading extends StatelessWidget {
  const PdfLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
